#Notes for further work

- I think the web server thread is "blocking", so that the "Just answer me, dammit" request cannot reach it
- Scrolling: When LLM output/thinking is shown, scrolling is ... weird. Fixable by wrapping in containers?